import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { UtilButton } from './UtilButton.js';

<Meta title="Utilities/Button" component={UtilButton} />

export const Template = (args) => <UtilButton {...args} />;

# Button

With `MDX`, we can define a story for `UtilButton` right in the middle of our
Markdown documentation.